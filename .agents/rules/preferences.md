# User Preferences

## Terminal / Commands
- **Never show `cmd /c` or raw PowerShell/shell command syntax in responses.** Run dev servers and commands silently in the background without surfacing shell invocation details to the user.
- When starting the dev server, just confirm it's running — do not display the command used.

## Dev Server
- The project at `d:\Testing\Website\website1` uses Next.js.
- Dev server runs on **http://localhost:3000**.
